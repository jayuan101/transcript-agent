# ── Core web framework ────────────────────────────────────────────────────────
anthropic>=0.52.0
gradio>=6.0.0
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9

# ── Environment / utilities ───────────────────────────────────────────────────
python-dotenv>=1.0.0
requests>=2.31.0
tqdm>=4.65.0

# ── Multi-provider LLM ────────────────────────────────────────────────────────
openai>=1.0.0
groq>=0.9.0

# ── Cloud STT engines (all optional — guarded by try/except) ─────────────────
deepgram-sdk>=3.2.0,<4.0.0
assemblyai>=0.17.0
elevenlabs>=1.0.0
rev_ai>=2.17.0
# google-cloud-speech>=2.20.0          # uncomment for Google Cloud STT
# azure-cognitiveservices-speech>=1.35.0  # uncomment for Azure Speech

# ── Document generation ───────────────────────────────────────────────────────
python-docx>=1.1.0
fpdf2>=2.7.0
pdfplumber>=0.11.0

# ── Local Whisper STT ─────────────────────────────────────────────────────────
openai-whisper>=20231117
tiktoken>=0.5.0
regex>=2023.0.0

# ── Audio / video ─────────────────────────────────────────────────────────────
# imageio-ffmpeg bundles ffmpeg on Windows — no system install needed
imageio-ffmpeg>=0.4.9
opencv-contrib-python-headless>=4.9.0
mediapipe>=0.10.0
deepface>=0.0.93
matplotlib>=3.8.0

# ── Video Interview Analysis ──────────────────────────────────────────────────
# Core (required for Video Analysis tab)
opencv-python>=4.8.0
mediapipe>=0.10.0
# Emotion detection backends (optional — install at least one for best accuracy)
# deepface>=0.0.93        # most accurate; pulls in TensorFlow (~2 GB)
# fer>=22.5.1             # lighter alternative (TF-based)
# plotly is already a Gradio transitive dep; add explicitly for clarity
plotly>=5.0.0

# ── PyTorch (CPU) — installed separately in setup scripts for size reasons ───
# torch  — handled by setup_windows.bat / setup_mac.sh
